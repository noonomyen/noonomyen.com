```sh
npm --prefix ../dev/build-tools/page <COMMAND>
```

```
build.ts [target->] [option]

target:
    target->error-page    -- /frontend/error-page

option:
    --release             -- replace src link and minify
```